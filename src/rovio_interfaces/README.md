# rovio_interfaces
Custom ROVIO  messages and services in ROS2.

To add a new msg type, add a .msg file in expected ros2 format in the msg folder.
To add a new srv type, add a .srv file in expected ros2 format in the srv folder.

# Current msgs supported

* Custom HealthMonitoring message.

# Current services supported

* Custom reset to external pose service
